version: '2'
services:
  postgres:
    image: 'postgis/postgis:latest'
    restart: always
    volumes:
      - './gasdata:/var/lib/postgresql/data'
    environment:
      - POSTGRES_PASSWORD=password
    ports:
      - '5432:5432'
